func main(): int {
  let a: int = (((14 + (0 - 21)) / 2) + (((0 - 26) ^ 38) - (16 + 22)));
  let b: int = ((18 >> 2) / 2);
  let c: int = (a + b);
  if c != 0 then {
    print c;
  } else {
    print 0;
  }
  return 0;
}
