include "../../src/stdlib/map.pyrel"

func main(): int {
  let map: map::HashMap<int, int> = map::with_capacity(4, 0, 0);
  map = map::put(map, 7, 70);
  map = map::put(map, 9, 90);
  print map::get_or(map, 7, (0 - 1));
  print map::get_or(map, 8, (0 - 1));
  map = map::free(map);
  return 0;
}
